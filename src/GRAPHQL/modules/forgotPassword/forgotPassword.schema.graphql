# import Error from "../../shared.graphql"

type Query {
    dummy3: String
}

type Mutation {
    forgotPassword(email: String): Boolean,
    changePassword(newPassword: String, key: String): Boolean,
}

